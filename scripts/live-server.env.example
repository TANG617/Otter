# Copy this file outside the repository, restrict its permissions, fill it there,
# then source that private copy. Never edit or commit credentials in this example.
export OTTER_RUN_LIVE_SERVER_TESTS=YES
export OTTER_TEST_SERVER_URL=
export OTTER_TEST_API_KEY=
